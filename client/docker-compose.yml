services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: front-end
    ports:
      - 5173:5173
    env_file:
      - .env

    networks:
      - workflow-network

networks:
  workflow-network:
    external: true
